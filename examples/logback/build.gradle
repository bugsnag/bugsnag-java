apply plugin: 'application'

buildscript {
    repositories {
        mavenCentral()
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile("ch.qos.logback:logback-classic:1.2.3")
    compile project(':bugsnag')
}

mainClassName = "com.bugsnag.example.logback.cli.Application"
