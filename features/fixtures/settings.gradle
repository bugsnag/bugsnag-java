include ':mazerunner',
        ':mazerunnerplainspring',
        ':mazerunnerspringboot',
        ':scenarios'

if (JavaVersion.hasProperty('VERSION_17') && JavaVersion.current() >= JavaVersion.VERSION_17) {
    include ':mazerunnerspringboot3',
            ':mazerunnerplainspring6'
}